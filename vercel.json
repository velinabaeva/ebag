{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm ci --prefix ./paper-live",
  "buildCommand": "npm run build --prefix ./paper-live",
  "outputDirectory": "paper-live/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
